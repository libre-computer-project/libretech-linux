name: C/C++ CI

on:
  push:
    branches: [ "linux-6.6.y-lc" ]
  pull_request:
    branches: [ "linux-6.6.y-lc" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
